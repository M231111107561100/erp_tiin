{
  "ConnectionStrings": {
    "Default": "Host=db;Port=${POSTGRES_PORT};Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}"
  },
  "Jwt": {
    "Issuer": "${JWT_ISSUER}",
    "Audience": "${JWT_AUDIENCE}",
    "JwksUri": "${JWT_JWKS_URI}"
  },
  "Otlp": {
    "Endpoint": "${OTEL_EXPORTER_OTLP_ENDPOINT}"
  }
}
